import React from "react";
import { useDispatch } from "react-redux";
import { addOrder } from "../store/productSlice";
const AddOrder = () => {
    const dispatch = useDispatch();
    
  return <div>AddOrder</div>;
};

export default AddOrder;
